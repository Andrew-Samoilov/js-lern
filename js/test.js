console.log(" -  * test * - ");
console.log(" -  * CodeWars *  * - ");
console.log();

{
    const add = (initValue) => {
        let result = initValue;

        const addRecursive = (value) => {
            if (!value) return result;

            result+=value;

            return addRecursive;
        }
    return addRecursive;
    }

const res-add(1)(2)(54)();
console.log(res);


}